import styled from 'styled-components';

const Login = (props) => {
    return (
        <div>
            <h1>Login</h1>
            <h1>hello from disney</h1>
        </div>
    );
};

export default Login;
